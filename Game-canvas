<!DOCTYPE html>
<html>
<head>
  <title>Drag Race Game</title>
  <style>
    /* Logo styling */
    .logo {
      font-family: "Helvetica Neue", Arial, sans-serif;
      font-weight: 900;
      font-size: 48px;
      text-transform: uppercase;
      background-image: linear-gradient(to right, #FF69B4, #FF00FF);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.4);
    }

    /* Start button styling */
    .start-button {
      font-family: "Helvetica Neue", Arial, sans-serif;
      font-size: 24px;
      text-transform: uppercase;
      padding: 12px 20px;
      background-color: #FF69B4;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      animation: pulse 2s infinite;
    }

    /* Animation keyframes */
    @keyframes pulse {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
      }
    }

    /* Main page background */
    body {
      background-image: url('https://i.ibb.co/bRz3XC7/background.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    }
  </style>
</head>
<body>
  <h1 class="logo">Drag Race Game</h1>
  <button class="start-button">Start Game</button>
</body>
</html>
